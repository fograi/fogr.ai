---
phase: 04-engagement-and-retention
plan: 02
type: execute
wave: 2
depends_on: ['04-01']
files_modified:
  - src/lib/components/AdCard.svelte
  - src/lib/components/AdCardWide.svelte
  - src/routes/+page.server.ts
  - src/routes/api/ads/+server.ts
  - src/routes/(public)/[category=category]/+page.server.ts
  - src/routes/(public)/[county=county]/+page.server.ts
  - src/routes/(public)/[category=category]/[county=county]/+page.server.ts
  - src/routes/(public)/category/[slug]/+page.server.ts
  - src/routes/(public)/ad/[slug]/+page.server.ts
  - src/routes/(public)/ad/[slug]/+page.svelte
autonomous: true
requirements: [TRST-01, ENGR-07]

must_haves:
  truths:
    - "Every ad card on the homepage shows relative time like 'Dublin . 2d ago'"
    - 'Every ad card on category and county pages shows relative time'
    - "Ad detail page shows full date: 'Posted 12 March 2026'"
    - 'Sold ads show a SOLD badge overlay on AdCard and AdCardWide'
    - 'Recently sold ads (within 7 days) appear in browse results with SOLD badge'
    - "Sale price displays as 'Sold for X' on the ad detail page when present"
  artifacts:
    - path: 'src/lib/components/AdCard.svelte'
      provides: 'Timestamp display and sold badge'
      contains: 'formatRelativeTime'
    - path: 'src/lib/components/AdCardWide.svelte'
      provides: 'Timestamp display and sold badge for wide cards'
      contains: 'formatRelativeTime'
    - path: 'src/routes/api/ads/+server.ts'
      provides: 'Sold ads included in browse results within 7-day window'
      contains: 'status.eq.sold'
  key_links:
    - from: 'src/lib/components/AdCard.svelte'
      to: 'src/lib/utils/relative-time.ts'
      via: 'import formatRelativeTime'
      pattern: 'import.*formatRelativeTime'
    - from: 'src/routes/api/ads/+server.ts'
      to: 'browse query'
      via: '7-day sold window filter'
      pattern: 'status.eq.sold.*updated_at'
    - from: 'src/routes/+page.server.ts'
      to: 'AdCard type'
      via: 'createdAt mapping from created_at'
      pattern: 'createdAt.*created_at'
---

<objective>
Add posted timestamps and sold badges to all ad cards and listing pages. Update browse queries to include recently sold ads (7-day window).

Purpose: Timestamps are a trust signal per TRST-01 ("Posted X days ago" visible on all listings). Sold badges with the 7-day visibility window are the display layer for ENGR-07.
Output: Updated AdCard/AdCardWide components, updated API query, updated all page server loaders.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-engagement-and-retention/04-CONTEXT.md
@.planning/phases/04-engagement-and-retention/04-RESEARCH.md
@.planning/phases/04-engagement-and-retention/04-01-SUMMARY.md
@src/lib/components/AdCard.svelte
@src/lib/components/AdCardWide.svelte
@src/types/ad-types.d.ts
@src/routes/api/ads/+server.ts
@src/routes/+page.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdCard and AdCardWide - timestamps and sold badges</name>
  <files>src/lib/components/AdCard.svelte, src/lib/components/AdCardWide.svelte</files>
  <action>
    **AdCard.svelte:**
    Add two new optional props with defaults (CRITICAL: all new props must have defaults to avoid breaking existing callers):
    - `export let createdAt: string | undefined = undefined;`
    - `export let status: string | undefined = undefined;`
    - `export let salePrice: number | null = null;`

    Import `formatRelativeTime` from `$lib/utils/relative-time`.

    Compute reactive values:
    - `$: timeAgo = createdAt ? formatRelativeTime(createdAt) : '';`
    - `$: isSold = status === 'sold';`

    Update the location line to include timestamp with dot separator per user decision:
    - Currently: `{#if locationSummary}<p class="location">{locationSummary}</p>{/if}`
    - Change to: `{#if locationSummary || timeAgo}<p class="location">{locationSummary}{#if locationSummary && timeAgo} &middot; {/if}{timeAgo}</p>{/if}`

    Add sold badge: When `isSold`, render a SOLD overlay badge in the card header area (next to category), using the existing `.badge` CSS class pattern but with a distinct sold style:
    ```
    {#if isSold}
      <div class="badges"><span class="badge sold">SOLD</span></div>
    {/if}
    ```
    Add CSS for `.badge.sold`: `background: color-mix(in srgb, var(--fg) 90%, var(--bg)); color: var(--bg); font-weight: 900;` -- prominent dark badge, reversed colors for maximum visibility.

    **AdCardWide.svelte:**
    Apply the same pattern. Read AdCardWide.svelte first to understand its structure, then add:
    - Same three optional props with defaults
    - Same import and reactive values
    - Timestamp in metadata area with dot separator
    - Sold badge in the appropriate location
    - Same CSS

    IMPORTANT: Both components must continue to work when these new props are NOT passed. Existing callers that spread `{...ad}` will automatically pick up new fields once the server loaders are updated.

  </action>
  <verify>Run `npx tsc --noEmit 2>&1 | head -30` to verify no type errors. Visually inspect both files have the new props, imports, and template changes.</verify>
  <done>Both AdCard and AdCardWide accept createdAt, status, salePrice props with defaults. Timestamps render with dot separator. Sold badge renders when status is 'sold'. No existing callers break.</done>
</task>

<task type="auto">
  <name>Task 2: Update API query and all page server loaders</name>
  <files>src/routes/api/ads/+server.ts, src/routes/+page.server.ts, src/routes/(public)/[category=category]/+page.server.ts, src/routes/(public)/[county=county]/+page.server.ts, src/routes/(public)/[category=category]/[county=county]/+page.server.ts, src/routes/(public)/category/[slug]/+page.server.ts, src/routes/(public)/ad/[slug]/+page.server.ts, src/routes/(public)/ad/[slug]/+page.svelte</files>
  <action>
    **API query update (src/routes/api/ads/+server.ts):**
    The GET handler currently filters `.eq('status', PUBLIC_AD_STATUS)` where `PUBLIC_AD_STATUS = 'active'`. Also add `status` and `sale_price` to the select columns.

    Change the status filter to include recently sold ads using the 7-day window:
    ```typescript
    const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();
    // Replace .eq('status', PUBLIC_AD_STATUS) with:
    query = query.or(`status.eq.active,and(status.eq.sold,updated_at.gt.${sevenDaysAgo})`);
    ```
    Also ensure `status`, `sale_price`, and `updated_at` are included in the select statement.

    The API response already includes created_at -- verify it's in the select column list (it is: line 832).

    **Homepage server loader (src/routes/+page.server.ts):**
    In the `AdCard[]` mapping (line 76), add:
    - `createdAt: ad.created_at ?? undefined`
    - `status: ad.status ?? undefined`
    - `salePrice: ad.sale_price ?? null`

    **All programmatic page server loaders** -- apply the same mapping change to:
    - `src/routes/(public)/[category=category]/+page.server.ts`
    - `src/routes/(public)/[county=county]/+page.server.ts`
    - `src/routes/(public)/[category=category]/[county=county]/+page.server.ts`
    - `src/routes/(public)/category/[slug]/+page.server.ts`

    Read each file first to find the `AdCard[]` mapping block and add the three new fields.

    **Ad detail page server loader (src/routes/(public)/ad/[slug]/+page.server.ts):**
    In the single ad `AdCard` mapping (around line 99), add `createdAt`, `status`, and `salePrice`.

    **Ad detail page template (src/routes/(public)/ad/[slug]/+page.svelte):**
    Import `formatFullDate` from `$lib/utils/relative-time`.
    Add "Posted {date}" below or near the location on the ad detail view:
    ```svelte
    {#if data.ad.createdAt}
      <p class="posted-date">Posted {formatFullDate(data.ad.createdAt)}</p>
    {/if}
    ```
    Style `.posted-date` with muted text (similar to location style: `font-size: 0.85rem; color: color-mix(in srgb, var(--fg) 55%, transparent);`).

    If the ad is sold and has a salePrice, display "Sold for {formatted price}" prominently on the detail page. Use the existing `formatMoney` from price.ts:
    ```svelte
    {#if data.ad.status === 'sold' && data.ad.salePrice}
      <p class="sale-price">Sold for {formatMoney(data.ad.salePrice, data.ad.currency ?? 'EUR')}</p>
    {/if}
    ```
    Style `.sale-price` with bold, slightly larger text to emphasize the transparency differentiator.

  </action>
  <verify>Run `npx tsc --noEmit 2>&1 | head -30` to check compilation. Verify API query change: `grep -n 'status.eq.sold' src/routes/api/ads/+server.ts`. Verify homepage mapping: `grep -n 'createdAt' src/routes/+page.server.ts`.</verify>
  <done>All browse pages pass createdAt, status, and salePrice to AdCard. Ad detail page shows full posted date and sold price. API includes recently-sold ads in browse results with 7-day window. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no new errors
- `grep -rn 'createdAt' src/routes/` shows mapping in all page server loaders
- `grep -n 'formatRelativeTime' src/lib/components/AdCard.svelte` shows import
- `grep -n 'status.eq.sold' src/routes/api/ads/+server.ts` shows 7-day window
- `grep -n 'formatFullDate' src/routes/(public)/ad/*/+page.svelte` shows full date display
</verification>

<success_criteria>
Every ad listing page displays relative timestamps. Ad detail pages display full posted dates. Sold ads visible in browse for 7 days with SOLD badge. Sale price visible on sold ad detail page.
</success_criteria>

<output>
After completion, create `.planning/phases/04-engagement-and-retention/04-02-SUMMARY.md`
</output>
