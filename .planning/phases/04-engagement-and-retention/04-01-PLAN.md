---
phase: 04-engagement-and-retention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260313_000020_watchlist.sql
  - supabase/migrations/20260313_000021_ads_sale_price_currency.sql
  - src/lib/utils/relative-time.ts
  - src/lib/utils/currency.ts
  - src/types/ad-types.d.ts
  - src/lib/supabase.types.ts
autonomous: true
requirements: [ENGR-01, ENGR-07, TRST-01, TRST-02]

must_haves:
  truths:
    - 'watchlist table exists with user_id + ad_id unique constraint and RLS'
    - 'ads table has sale_price integer column (nullable)'
    - "formatRelativeTime() converts ISO dates to compact strings like '2d ago'"
    - "formatFullDate() converts ISO dates to 'Posted 12 March 2026'"
    - 'getDefaultCurrency() returns GBP for the 6 NI counties, EUR for everything else'
    - 'AdCard type includes createdAt, salePrice, and status fields'
  artifacts:
    - path: 'supabase/migrations/20260313_000020_watchlist.sql'
      provides: 'watchlist table with RLS'
      contains: 'CREATE TABLE'
    - path: 'supabase/migrations/20260313_000021_ads_sale_price_currency.sql'
      provides: 'sale_price column on ads'
      contains: 'sale_price'
    - path: 'src/lib/utils/relative-time.ts'
      provides: 'formatRelativeTime and formatFullDate utilities'
      exports: ['formatRelativeTime', 'formatFullDate']
    - path: 'src/lib/utils/currency.ts'
      provides: 'NI county to GBP default mapping'
      exports: ['getDefaultCurrency', 'NI_COUNTY_IDS']
    - path: 'src/types/ad-types.d.ts'
      provides: 'Updated AdCard and ApiAdRow types'
      contains: 'createdAt'
  key_links:
    - from: 'src/lib/utils/currency.ts'
      to: 'src/lib/data/ireland_counties.json'
      via: 'NI county ID set'
      pattern: 'ie/ulster/(antrim|armagh|down|fermanagh|londonderry|tyrone)'
---

<objective>
Create the database foundations and shared utilities for all Phase 4 features: watchlist table, sale_price column, relative time formatting, NI/GBP currency defaults, and updated TypeScript types.

Purpose: Every other Phase 4 plan depends on these migrations and utilities. This is the horizontal foundation that unblocks all vertical feature slices.
Output: 2 SQL migrations, 2 utility modules, updated type definitions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engagement-and-retention/04-CONTEXT.md
@.planning/phases/04-engagement-and-retention/04-RESEARCH.md
@src/types/ad-types.d.ts
@src/lib/utils/price.ts
@src/lib/data/ireland_counties.json
@supabase/migrations/20260312_000019_saved_searches.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migrations (watchlist table + sale_price column)</name>
  <files>supabase/migrations/20260313_000020_watchlist.sql, supabase/migrations/20260313_000021_ads_sale_price_currency.sql</files>
  <action>
    Create two SQL migration files:

    **Migration 000020 - Watchlist table:**
    Follow the exact pattern from `20260312_000019_saved_searches.sql`. Create `public.watchlist` table:
    - `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`
    - `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`
    - `ad_id uuid NOT NULL REFERENCES public.ads(id) ON DELETE CASCADE`
    - `created_at timestamptz NOT NULL DEFAULT now()`
    - `UNIQUE(user_id, ad_id)` constraint
    - Index on `user_id` for fast user lookups
    - Enable RLS with policy "Users manage own watchlist" for ALL operations to authenticated users, using `auth.uid() = user_id` for both USING and WITH CHECK

    **Migration 000021 - sale_price column + currency update:**
    - `ALTER TABLE public.ads ADD COLUMN IF NOT EXISTS sale_price integer` (nullable, cents not euros -- matches existing price column pattern)
    - Verify no CHECK constraint exists on the ads currency column that would block 'GBP'. The existing validation in ads-validation.ts uses `/^[A-Z]{3}$/` which already allows GBP, so no DB constraint change needed. If a constraint does exist, alter it to allow both EUR and GBP. Add a comment in the migration noting this was verified.

    Follow existing migration naming pattern exactly. Use `IF NOT EXISTS` for idempotency.

  </action>
  <verify>Run `cat supabase/migrations/20260313_000020_watchlist.sql` and `cat supabase/migrations/20260313_000021_ads_sale_price_currency.sql` to verify SQL syntax. Check that RLS is enabled and policies are created.</verify>
  <done>Both migration files exist with valid SQL. Watchlist table has unique constraint, RLS, and user_id index. Ads table has sale_price column.</done>
</task>

<task type="auto">
  <name>Task 2: Shared utilities and type updates</name>
  <files>src/lib/utils/relative-time.ts, src/lib/utils/currency.ts, src/types/ad-types.d.ts, src/lib/supabase.types.ts</files>
  <action>
    **relative-time.ts** -- Create utility using native `Intl.RelativeTimeFormat`:
    - `formatRelativeTime(isoDate: string): string` -- Returns compact relative time: "2d ago", "3h ago", "just now" (for less than 1 minute). Use `Intl.RelativeTimeFormat('en', { numeric: 'always', style: 'narrow' })`. Walk units from year down to minute, return first match.
    - `formatFullDate(isoDate: string): string` -- Returns "12 March 2026" using `Intl.DateTimeFormat('en-IE', { day: 'numeric', month: 'long', year: 'numeric' })`.
    - Use the code example from 04-RESEARCH.md as the reference implementation.

    **currency.ts** -- Create NI county default currency mapping:
    - `NI_COUNTY_IDS` Set containing the 6 Northern Ireland county IDs from ireland_counties.json: `ie/ulster/antrim`, `ie/ulster/armagh`, `ie/ulster/down`, `ie/ulster/fermanagh`, `ie/ulster/londonderry`, `ie/ulster/tyrone`. NOTE: Do NOT include the 3 ROI Ulster counties (Cavan, Donegal, Monaghan) per user decision.
    - `getDefaultCurrency(countyId: string | null): 'EUR' | 'GBP'` -- Returns GBP if countyId is in NI_COUNTY_IDS, EUR otherwise.
    - Export both for reuse in post form and display logic.

    **ad-types.d.ts** -- Update types:
    - Add `createdAt?: string` to `AdCard` interface (for timestamp display, maps from `created_at`)
    - Add `salePrice?: number | null` to `AdCard` interface (for sold price display)
    - Add `sale_price?: number | null` to `ApiAdRow` type (matches DB column)
    - Ensure `status?: string` already exists on AdCard (it does -- verify)

    **supabase.types.ts** -- Manually add `sale_price: number | null` to the ads table Row, Insert, and Update types. Add `watchlist` table types following the existing pattern. Note: Full type regeneration deferred per STATE.md pending todo -- just add the minimum needed fields.

  </action>
  <verify>Run `npx tsc --noEmit 2>&1 | head -30` to check for type errors introduced. Verify new exports: `grep -n 'export' src/lib/utils/relative-time.ts src/lib/utils/currency.ts`.</verify>
  <done>Both utility files export their functions. AdCard has createdAt and salePrice fields. ApiAdRow has sale_price. TypeScript compilation has no new errors from these changes.</done>
</task>

</tasks>

<verification>
- Both migration files exist with valid SQL
- `formatRelativeTime('2026-03-11T00:00:00Z')` returns a string containing "ago" or "just now"
- `getDefaultCurrency('ie/ulster/antrim')` returns 'GBP'
- `getDefaultCurrency('ie/leinster/dublin')` returns 'EUR'
- `getDefaultCurrency('ie/ulster/donegal')` returns 'EUR' (ROI Ulster county)
- AdCard type includes createdAt, salePrice, status
- No new TypeScript compilation errors
</verification>

<success_criteria>
All Phase 4 database migrations and shared utilities exist and compile. Every subsequent plan can import these modules and reference these DB columns without creating them.
</success_criteria>

<output>
After completion, create `.planning/phases/04-engagement-and-retention/04-01-SUMMARY.md`
</output>
