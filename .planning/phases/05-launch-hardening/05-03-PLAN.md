---
phase: 05-launch-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+page.svelte
  - src/routes/(public)/about/+page.svelte
  - src/routes/+layout.svelte
autonomous: true
requirements:
  - TRST-07

must_haves:
  truths:
    - "The homepage hero displays private-seller trust messaging with the simplicity angle: 'Honest ads. Fair prices. No clutter.'"
    - "The /about page tells the brand story including 'Fograi' Irish-language meaning, private-seller-only policy, community positioning, and who built it"
    - 'The footer includes a Safety link pointing to /safety and updated trust-aligned copy'
    - "Browse pages display anti-dealer messaging: 'No dealers. No middlemen. Real people selling real things.'"
  artifacts:
    - path: 'src/routes/+page.svelte'
      provides: 'Updated hero copy with trust messaging + anti-dealer tagline on browse'
      contains: 'Honest ads'
    - path: 'src/routes/(public)/about/+page.svelte'
      provides: 'Rewritten about page with brand story, mission, private-seller policy'
      contains: 'Fograi'
    - path: 'src/routes/+layout.svelte'
      provides: 'Footer with Safety link and trust-aligned copy'
      contains: '/safety'
  key_links:
    - from: 'src/routes/+layout.svelte'
      to: '/safety'
      via: 'anchor href'
      pattern: 'href.*safety'
---

<objective>
Add private-seller trust messaging throughout the platform -- homepage hero, about page rewrite, footer updates, and browse page tagline.

Purpose: Position fogr.ai as the honest, private-seller-only alternative to Facebook Marketplace. Every key page reinforces this identity.
Output: Updated homepage hero copy, rewritten about page with brand story, footer with safety link and trust copy.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-launch-hardening/05-CONTEXT.md
@.planning/phases/05-launch-hardening/05-RESEARCH.md
@src/routes/+page.svelte
@src/routes/(public)/about/+page.svelte
@src/routes/+layout.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage hero copy and add anti-dealer tagline to browse area</name>
  <files>src/routes/+page.svelte</files>
  <action>
This file uses Svelte 5 syntax (`$props()`, `$derived`, `$state`, `onclick`). Match existing patterns.

**1. Update hero copy:**

Find the hero section (the main heading area of the homepage). The current hero likely has a heading and subtitle. Update the subtitle/description to use the simplicity angle per CONTEXT.md:

Change the existing subtitle/subheading text to: `"Honest ads. Fair prices. No clutter."`

Keep the main `<h1>` as-is if it already works (e.g., "Buy. Sell. Done."). Only change the subtitle/description paragraph.

If there's a longer description paragraph, update it to: `"Second-hand goods from real people across Ireland. No dealers, no clutter -- just honest listings."` This reinforces both the private-seller and simplicity angle.

**2. Add anti-dealer tagline to browse section:**

Find the area where browse results are displayed (the ad grid section). Add a subtle tagline above the results grid (below filters, above the ad cards):

```svelte
<p class="browse-tagline">No dealers. No middlemen. Real people selling real things.</p>
```

Style it subtle and unobtrusive -- small font (0.85rem), muted color, centered, with small top/bottom margin. It should feel like a quiet brand statement, not a shouting banner:

```css
.browse-tagline {
	text-align: center;
	font-size: 0.85rem;
	color: color-mix(in srgb, var(--fg) 50%, transparent);
	margin: 0 0 12px;
	font-style: italic;
}
```

Only show the tagline when there are results (not on empty states).
</action>
<verify>

- `npm run build` succeeds
- Homepage hero contains "Honest ads. Fair prices. No clutter." text
- Browse section shows the anti-dealer tagline when results exist
  </verify>
  <done>
- Homepage hero updated with private-seller simplicity messaging
- Browse results area shows subtle anti-dealer tagline
- All copy changes match the newspaper-simple brand tone
  </done>
  </task>

<task type="auto">
  <name>Task 2: Rewrite about page with brand story and update footer with safety link</name>
  <files>src/routes/(public)/about/+page.svelte, src/routes/+layout.svelte</files>
  <action>
**1. Rewrite `src/routes/(public)/about/+page.svelte`:**

This is an existing page (NOT a new route). Read the current content first, then rewrite. The existing about page uses Svelte 4 implicit syntax (no script block at all, just template + style). Keep it that way -- no script block needed.

Rewrite with these sections per CONTEXT.md:

**a) Hero section:**

- Kicker: "About Fogr.ai -- Fograi"
- H1: "Your local noticeboard, online."
- Lead paragraph explaining the mission: what fogr.ai is, the Irish-language origin of the name ("Fograi" -- Irish for "advertisements" or "notices"), and why it exists.

**b) "What makes this different" section** with card grid (preserve existing grid/card styles):

Cards should cover:

- **Private sellers only**: "No dealers, no showrooms, no commercial listings. Every ad is from a real person selling their own things."
- **Clear, honest listings**: "Required fields and validation keep listings readable, complete, and quick to create."
- **Price transparency**: "Fixed, Free, and POA prices enforced by category. No hidden costs, no bait-and-switch."
- **Your privacy, your terms**: "Messages are anonymous in-app. Share contact details only if you choose to."
- **Scam awareness built in**: "Safety tips on every ad, messages checked for common scam signals, and a proper reporting flow."
- **An Irish platform for Irish people**: "Built in Ireland, for Ireland. All 32 counties, North and South."

**c) Mission / Why section:**

- Heading: "Why we built this"
- Brief paragraph about the frustration with Facebook Marketplace (dealers posing as private sellers, cluttered interface, no accountability). Position fogr.ai as the honest alternative.
- Make it personal but not bitter. Tone: "We wanted somewhere simple and honest to buy and sell locally."

**d) Private-seller policy:**

- Heading: "Private sellers only"
- Short paragraph explaining the policy: trade and commercial listings are not allowed and will be removed. This keeps the platform honest and fair.

**e) Contact section** (preserve existing):

- Email: eolas@fogr.ai
- Links to browse and post

Add `<svelte:head>` block with:

```svelte
<svelte:head>
	<title>About | fogr.ai -- Fograi</title>
	<meta
		name="description"
		content="Fogr.ai is an Irish classifieds platform for private sellers. No dealers, no clutter -- just honest ads from real people across all 32 counties."
	/>
</svelte:head>
```

Preserve existing CSS structure (max-width, grid, card styles). Update the kicker and heading styles. Keep the newspaper aesthetic.

**2. Modify `src/routes/+layout.svelte`:**

This file uses Svelte 5 syntax (`$props()`, `$page` from stores). Match existing patterns.

**a) Add Safety link to the footer "Support" nav group:**

Find the "Support" `<nav>` group (around line 37-39). Add a Safety link:

```svelte
<nav class="link-group" aria-label="Support">
	<p class="label">Support</p>
	<a href={resolve('/(public)/safety')}>Safety tips</a>
	<a href={resolve('/(public)/report-status')}>Report status</a>
</nav>
```

**b) Update the brand line** in the footer to add a subtle trust statement. After the `<span>` with the copyright year, add:

```svelte
<span class="dot">*</span>
<span class="trust-line">Private sellers only</span>
```

Style `.trust-line` to be subtle -- small, muted, matching the existing footer aesthetic:

```css
.trust-line {
	font-size: 0.75rem;
	font-weight: 400;
	color: color-mix(in srgb, var(--fg) 50%, transparent);
	letter-spacing: 0.04em;
}
```

  </action>
  <verify>
- `npm run build` succeeds
- /about page contains "Fograi", "Private sellers only", Irish identity messaging
- /about page has proper `<title>` and meta description
- Footer contains "Safety tips" link pointing to /safety
- Footer brand line includes "Private sellers only" trust message
  </verify>
  <done>
- About page rewritten with brand story, mission, Irish-language origin, private-seller policy
- Footer includes Safety tips link in Support nav group
- Footer brand line subtly reinforces private-seller-only positioning
- All copy maintains newspaper-simple, honest tone
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Homepage hero displays simplicity-angle trust messaging
- Browse area shows anti-dealer tagline
- /about page tells the full brand story with Irish identity and private-seller policy
- Footer links to /safety and displays trust messaging
</verification>

<success_criteria>

- Private-seller trust messaging displayed prominently across homepage, about page, browse pages, and footer
- Three positioning angles covered: anti-dealer (browse), simplicity (homepage), community (about)
- Brand name "Fogr.ai -- Fograi" used on key pages per CONTEXT.md
- All copy maintains the newspaper-simple, honest tone
  </success_criteria>

<output>
After completion, create `.planning/phases/05-launch-hardening/05-03-SUMMARY.md`
</output>
