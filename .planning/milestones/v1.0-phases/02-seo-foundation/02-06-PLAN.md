---
phase: 02-seo-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/(public)/[county=county]/+page.svelte
autonomous: true
gap_closure: true
requirements:
  - SEO-06

must_haves:
  truths:
    - 'County pages (/dublin, /cork, etc.) include og:image meta tag pointing to a valid OG fallback image'
    - 'County pages include twitter:image meta tag matching the og:image value'
    - 'Social sharing previews for county pages display an image instead of a blank/broken preview'
  artifacts:
    - path: 'src/routes/(public)/[county=county]/+page.svelte'
      provides: 'Complete OG and Twitter Card meta tags including image'
      contains: 'og:image'
  key_links:
    - from: 'src/routes/(public)/[county=county]/+page.svelte'
      to: 'static/og-fallback/home-garden.png'
      via: 'og:image content attribute'
      pattern: 'og:image.*og-fallback'
---

<objective>
Add missing og:image and twitter:image meta tags to the county-only programmatic page.

Purpose: Close the single verification gap from Phase 2. County pages (/dublin, /cork, etc.) currently have twitter:card set to summary_large_image but no image URL, causing broken social sharing previews on all social media and messaging platforms. The category page and category+county page both include og:image; the county page must be made consistent.

Output: Updated county page svelte component with complete OG tags.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seo-foundation/02-VERIFICATION.md

@src/routes/(public)/[county=county]/+page.svelte
@src/routes/(public)/[category=category]/+page.svelte (reference for og:image pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add og:image and twitter:image to county page</name>
  <files>src/routes/(public)/[county=county]/+page.svelte</files>
  <action>
Add two meta tags to the svelte:head block in src/routes/(public)/[county=county]/+page.svelte.

Insert after line 34 (the og:site_name tag) and before line 35 (the twitter:card tag):

```svelte
<meta
	property="og:image"
	content="{data.seo.canonical.split('/').slice(0, 3).join('/')}/og-fallback/home-garden.png"
/>
```

Insert after the twitter:description tag (currently line 37), before the JSON-LD block:

```svelte
<meta
	name="twitter:image"
	content="{data.seo.canonical.split('/').slice(0, 3).join('/')}/og-fallback/home-garden.png"
/>
```

This uses `home-garden.png` as the generic fallback because county pages are not category-specific. This matches the pattern used by `buildHomepageOg()` and `buildCategoryOg()` default behavior. The origin derivation technique (`canonical.split('/').slice(0, 3).join('/')`) is identical to what the category page and category+county page already use.

Do NOT change any other lines in the file. This is a two-line addition only.
</action>
<verify>

1. Run `grep -n 'og:image\|twitter:image' src/routes/(public)/[county=county]/+page.svelte` -- should show exactly 2 lines (one og:image, one twitter:image), both pointing to og-fallback/home-garden.png
2. Run `grep -c 'og:image' src/routes/(public)/[county=county]/+page.svelte` -- should return 1
3. Run `grep -c 'twitter:image' src/routes/(public)/[county=county]/+page.svelte` -- should return 1
4. Run `npm run build` (or equivalent) -- build succeeds with no errors
   </verify>
   <done>
   County page svelte:head contains og:image pointing to /og-fallback/home-garden.png and twitter:image with the same value. The meta tags use the same origin-derivation pattern as the category page. Build passes.
   </done>
   </task>

</tasks>

<verification>
1. Grep confirms og:image and twitter:image present in county page svelte file
2. Compare county page head block with category page head block -- both now have og:title, og:description, og:url, og:type, og:site_name, og:image, twitter:card, twitter:title, twitter:description, twitter:image
3. Build passes without errors
4. SEO-06 requirement (OG and Twitter Card meta tags) is now fully satisfied across all page types
</verification>

<success_criteria>

- og:image meta tag present in county page svelte:head block
- twitter:image meta tag present in county page svelte:head block
- Both point to a valid static OG fallback image path
- Build compiles without errors
- County page OG tags are consistent with category page and category+county page patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/02-seo-foundation/02-06-SUMMARY.md`
</output>
