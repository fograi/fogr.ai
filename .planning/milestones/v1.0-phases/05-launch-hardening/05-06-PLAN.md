---
phase: 05-launch-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/api/ads/+server.ts
autonomous: true
gap_closure: true
requirements:
  - LNCH-01

must_haves:
  truths:
    - 'A text-only ad posted by a new account (fewer than 3 approved ads OR younger than 7 days) is set to pending status, not active'
    - 'The ad is not publicly visible in browse results until the cron worker re-processes and auto-approves it'
    - 'Established accounts (3+ approved ads AND 7+ days old) are unaffected -- their text-only ads still go directly to active'
    - 'New-account pending ads have moderation_hold_reason = null so the cron worker picks them up for auto-approval'
    - 'The response message for new-account held ads indicates the ad is pending review'
  artifacts:
    - path: 'src/routes/api/ads/+server.ts'
      provides: 'isNewAccount() integration in POST handler for pending hold'
      contains: 'isNewAccount'
  key_links:
    - from: 'src/routes/api/ads/+server.ts'
      to: 'src/lib/server/new-account.ts'
      via: 'import isNewAccount'
      pattern: 'import.*isNewAccount.*from.*new-account'
---

<objective>
Close LNCH-01 verification gap: wire isNewAccount() into the ad POST handler so text-only ads from new accounts are held in pending status until cron-worker re-processes them.

Purpose: The verification report found that isNewAccount() exists and works correctly but is only imported by page.server.ts (for noindex). It is NOT called in the POST handler. Text-only ads from new accounts pass synchronous AI moderation and go directly to active status, making them publicly visible immediately. This violates LNCH-01 which requires "held in pending state until moderation completes before becoming publicly visible."

Output: Modified POST handler that forces status to 'pending' for new-account ads. The cron worker already handles pending ads with null moderation_hold_reason -- it will pick these up and auto-approve them if they pass AI moderation. This satisfies both the user's decision ("no separate hold queue, existing pending->active flow is sufficient") and LNCH-01.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launch-hardening/05-VERIFICATION.md
@.planning/phases/05-launch-hardening/05-01-SUMMARY.md
@src/lib/server/new-account.ts
@src/routes/api/ads/+server.ts
@src/cron-worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire isNewAccount() into ad POST handler for pending hold</name>
  <files>src/routes/api/ads/+server.ts</files>
  <action>
  Import `isNewAccount` from `$lib/server/new-account` at the top of the file alongside the existing imports.

After the service-role client construction (around line 443-450 where `limiterClient` is created), call `isNewAccount(limiterClient, user.id)` to determine whether the poster is a new account. The `limiterClient` already uses the service-role key when available, which is required for `supabase.auth.admin.getUserById()` inside isNewAccount(). Store the result in a `const isNewAccountUser` variable.

IMPORTANT: Place this call AFTER the daily rate limit check (line 452-474) but BEFORE the local moderation checks. This avoids an extra DB call for users who are already over their daily limit. The exact placement should be after line 474 (after the rate limit guard returns) and before line 478 (the local moderation section).

Modify the status determination line (currently line 545):

```
// BEFORE:
const status = moderationUnavailable || isResellerFlagged ? 'pending' : PUBLIC_AD_STATUS;

// AFTER:
const status = moderationUnavailable || isResellerFlagged || isNewAccountUser ? 'pending' : PUBLIC_AD_STATUS;
```

The `moderation_hold_reason` in the insert (line 588) should NOT be set for new-account holds. It should remain:

```
moderation_hold_reason: isResellerFlagged ? 'reseller_flagged' : null
```

This is correct as-is because new-account pending ads should have `moderation_hold_reason = null`, which means the cron worker WILL pick them up and auto-approve after AI moderation re-check. This matches the user's decision: "no separate hold queue -- existing pending->active flow via AI moderation is sufficient."

Update the response message logic (currently line 711-713) to also indicate pending status for new-account holds:

```
// BEFORE:
const responseMessage = moderationUnavailable
  ? 'Ad submitted and pending review.'
  : 'Ad submitted.';

// AFTER:
const responseMessage = moderationUnavailable || isNewAccountUser
  ? 'Ad submitted and pending review.'
  : 'Ad submitted.';
```

Note: reseller-flagged ads intentionally do NOT get a "pending review" message (silent flagging per user decision). Only moderationUnavailable and isNewAccountUser get the pending message.

Add a log entry for new-account holds (after the reseller flagging log block, around line 497):

```
if (isNewAccountUser && !isResellerFlagged) {
  log('info', 'ads_post_new_account_hold', {
    userId: user.id,
    requestId
  });
}
```

Only log when the new-account flag is the reason (not when already flagged as reseller, which has its own log).

Also update the image bucket selection logic (line 627) to use pending bucket for new-account ads:

```
// BEFORE:
const targetBucket = moderationUnavailable ? pendingBucket : publicBucket;
const cacheControl = moderationUnavailable
  ? 'private, max-age=86400'
  : 'public, max-age=31536000, immutable';

// AFTER:
const targetBucket = (moderationUnavailable || isNewAccountUser) ? pendingBucket : publicBucket;
const cacheControl = (moderationUnavailable || isNewAccountUser)
  ? 'private, max-age=86400'
  : 'public, max-age=31536000, immutable';
```

Wait -- this needs careful thought. For reseller-flagged ads, the existing code already handles bucket routing via the `moderationUnavailable` flag. But reseller-flagged text-only ads that passed moderation go to public bucket even though status is pending. Let me check...

Actually, looking at the code again: `isResellerFlagged` does NOT affect bucket selection (line 627 only checks `moderationUnavailable`). This means reseller-flagged text-only ads (status=pending but moderation passed) have images in the public bucket, which is fine because they're approved content waiting for manual review.

For new-account text-only ads: they also passed synchronous AI moderation, so their content is clean. Images going to public bucket is acceptable -- the ad won't be publicly browsable until cron approves it (status is pending, browse queries filter by status=active). So do NOT change the bucket logic for new-account holds. Leave lines 627-628 unchanged.

Summary of changes to src/routes/api/ads/+server.ts:

1. Add import: `import { isNewAccount } from '$lib/server/new-account';`
2. Add call after rate limit check: `const isNewAccountUser = await isNewAccount(limiterClient, user.id);`
3. Update status line: add `|| isNewAccountUser`
4. Add log entry for new-account holds
5. Update response message: add `|| isNewAccountUser`
6. Do NOT change moderation_hold_reason (leave null for new-account)
7. Do NOT change bucket selection logic
   </action>
   <verify>
8. `grep -n 'isNewAccount' src/routes/api/ads/+server.ts` shows import and usage (at least 3 matches: import, call, status line)
9. `grep -n 'isNewAccountUser' src/routes/api/ads/+server.ts` shows the variable used in status determination and response message
10. `grep -n "moderation_hold_reason.*reseller_flagged" src/routes/api/ads/+server.ts` still shows only reseller_flagged (no new_account hold reason)
11. `npx svelte-check --tsconfig ./tsconfig.json` passes without errors
12. `npm run lint` passes
    </verify>
    <done>

- isNewAccount() is imported and called in the POST handler with the service-role client
- New-account ads get status='pending' regardless of synchronous AI moderation result
- New-account pending ads have moderation_hold_reason=null so cron picks them up for auto-approval
- Established accounts are unaffected (isNewAccount returns false)
- Response message says "pending review" for new-account ads
- A log entry distinguishes new-account holds from reseller holds
- No changes to image bucket routing (images still go to public bucket for clean content)
  </done>
  </task>

</tasks>

<verification>
After completion, verify the full gap is closed:

1. **Code wiring:** `isNewAccount` is imported from `$lib/server/new-account` and called with the service-role `limiterClient`
2. **Status override:** The status determination line includes `isNewAccountUser` as a condition for pending
3. **Cron compatibility:** `moderation_hold_reason` is null for new-account holds, so cron worker's `moderation_hold_reason=is.null` filter will pick them up
4. **No regression:** Reseller detection, private-seller validation, and established-account flow are unchanged
5. **Build passes:** `npx svelte-check` and `npm run lint` both pass
   </verification>

<success_criteria>

- LNCH-01 is satisfied: ads from new accounts are held in pending state (not publicly visible in browse) until cron worker re-processes them
- The existing pending->active cron flow handles auto-approval (no separate hold queue, per user decision)
- Established account posting flow is unchanged
- Single file modified (src/routes/api/ads/+server.ts) with ~10 lines of changes
  </success_criteria>

<output>
After completion, create `.planning/phases/05-launch-hardening/05-06-SUMMARY.md`
</output>
