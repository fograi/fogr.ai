---
phase: 07-verification-and-requirements-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-slug-migration/01-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - 'Phase 01 has a VERIFICATION.md confirming all 5 success criteria are satisfied with code-level evidence'
    - 'SEO-01 appears as satisfied in the verification report'
    - 'All 38 REQUIREMENTS.md checkboxes show [x]'
    - 'All 38 traceability table rows show Satisfied'
  artifacts:
    - path: '.planning/phases/01-slug-migration/01-VERIFICATION.md'
      provides: 'Formal verification of Phase 01 slug migration with code-level evidence'
      contains: 'status: passed'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'Updated requirement checkboxes and traceability statuses'
      contains: '[x]'
  key_links:
    - from: '.planning/phases/01-slug-migration/01-VERIFICATION.md'
      to: 'ROADMAP.md Phase 1 success criteria'
      via: '5 observable truths mapping to 5 success criteria'
      pattern: 'VERIFIED'
    - from: '.planning/REQUIREMENTS.md'
      to: 'Traceability table'
      via: 'Status column update'
      pattern: 'Satisfied'
---

<objective>
Close the single remaining v1.0 milestone audit gap: formally verify Phase 01's slug migration and update all requirement documentation to reflect actual implementation status.

Purpose: Phase 01 was fully implemented but never formally verified. SEO-01 is orphaned in the audit. All 38 REQUIREMENTS.md checkboxes still show `[ ]` despite all requirements being satisfied. This plan creates the missing verification report and updates requirements documentation so the milestone can close.

Output: 01-VERIFICATION.md (Phase 01 formal verification), updated REQUIREMENTS.md (38 checkboxes + traceability table)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-verification-and-requirements-closure/07-CONTEXT.md
@.planning/phases/07-verification-and-requirements-closure/07-RESEARCH.md
@.planning/phases/02-seo-foundation/02-VERIFICATION.md

Key source files for verification evidence (re-read at execution time for current line numbers):
@src/lib/server/slugs.ts
@src/routes/(public)/ad/[slug]/+page.server.ts
@src/routes/api/ads/+server.ts
@scripts/backfill-slugs.ts
@supabase/migrations/20260312_000017_ads_slug_column.sql
@src/lib/components/AdCard.svelte
@src/types/ad-types.d.ts
@src/lib/supabase.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 01 VERIFICATION.md with code-level evidence</name>
  <files>.planning/phases/01-slug-migration/01-VERIFICATION.md</files>
  <action>
Create a VERIFICATION.md for Phase 01 (slug migration) following the EXACT format established by Phase 02's VERIFICATION.md (02-VERIFICATION.md). This is the reference template.

IMPORTANT: Re-read all source files cited below at execution time to get CURRENT line numbers. Do NOT hardcode the line numbers from 07-RESEARCH.md — they may have drifted.

**Frontmatter (YAML):**

```yaml
---
phase: 01-slug-migration
verified: { current ISO timestamp }
status: passed
score: X/X must-haves verified
re_verification: false
human_verification:
  - test: 'Run `SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npx tsx scripts/backfill-slugs.ts` against production'
    expected: 'Script reports backfill_complete with totalProcessed count. All pre-migration ads have slug and short_id populated.'
    why_human: 'Requires live Supabase connection and service role credentials. Note: database was emptied during verification (per STATE.md) — no pre-migration ads currently exist. New ads get slugs automatically via POST handler. Run defensively before launch as a safety check.'
---
```

**Body structure (match Phase 02 format exactly):**

1. Title: `# Phase 01: Slug Migration Verification Report`
2. Phase Goal from ROADMAP.md: "Ad pages use human-readable URLs so that all downstream SEO work has value from the moment it ships."
3. Verified timestamp + status

4. **Observable Truths table** — Map the 5 Phase 01 ROADMAP.md success criteria:

| # | Truth | Status | Evidence |
Each truth MUST cite: specific file path, current line numbers, and the relevant code pattern/function call. Read each source file to find evidence.

Truth 1: "Visiting /ad/{slug} opens the correct ad — URL contains title and short ID, not UUID"

- Evidence from: `src/routes/(public)/ad/[slug]/+page.server.ts` — find `parseSlugShortId` call and `.eq('short_id', shortId)` query

Truth 2: "Visiting an old UUID URL redirects with HTTP 301 to the new slug URL"

- Evidence from: `src/routes/(public)/ad/[slug]/+page.server.ts` — find `isUuidParam` check and `redirect(301, ...)` call

Truth 3: "New ads posted after migration automatically get a slug at insert time"

- Evidence from: `src/routes/api/ads/+server.ts` — find `generateAdSlug` import and call in POST handler

Truth 4: "Slug collisions handled automatically (identical titles get distinct slugs)"

- Evidence from: `src/routes/api/ads/+server.ts` — find retry loop with Postgres error code `23505` (unique constraint violation)

Truth 5: "No existing ad link becomes a permanent 404 — all resolve via redirect"

- Evidence from: `src/routes/(public)/ad/[slug]/+page.server.ts` — find UUID redirect AND canonical redirect (non-canonical slug resolves to canonical)

5. **Required Artifacts table** — Verify each artifact exists with correct exports/content:

- `src/lib/server/slugs.ts` — exports generateAdSlug, extractShortId, parseSlugShortId, isUuidParam
- `src/lib/server/slugs.spec.ts` — unit tests exist
- `supabase/migrations/20260312_000017_ads_slug_column.sql` — migration adds slug + short_id columns with unique indexes
- `scripts/backfill-slugs.ts` — imports generateAdSlug, batch processing
- `src/routes/(public)/ad/[slug]/+page.server.ts` — UUID redirect + slug routing
- `src/routes/api/ads/+server.ts` — POST handler with slug generation
- `src/lib/components/AdCard.svelte` — slug prop used in href
- `src/types/ad-types.d.ts` — AdCard type includes slug field
- `src/lib/supabase.types.ts` — slug + short_id in Row/Insert/Update types

6. **Key Link Verification table:**

- `ad/[slug]/+page.server.ts` -> `slugs.ts` via import { isUuidParam, parseSlugShortId }
- `api/ads/+server.ts` -> `slugs.ts` via import { generateAdSlug }
- `scripts/backfill-slugs.ts` -> `slugs.ts` via import { generateAdSlug }
- `AdCard.svelte` -> slug prop via href with resolve()
- `supabase.types.ts` -> ads table via slug/short_id in Row/Insert/Update types

7. **Requirements Coverage table:**
   | SEO-01 | Human-readable URL slugs | SATISFIED | Verified by all 5 observable truths above |

8. **Anti-Patterns Found table** — Note any issues found. If none, state "No anti-patterns found."

9. **Human Verification Required** — The backfill script item from frontmatter, expanded with the Test/Expected/Why human format.

10. Footer: `_Verified: {timestamp}_ _Verifier: Claude (gsd-verifier)_`

Do NOT update any SUMMARY.md files per user decision in CONTEXT.md.
</action>
<verify>
Verify the file exists and has correct structure:

1. `test -f .planning/phases/01-slug-migration/01-VERIFICATION.md` succeeds
2. File contains YAML frontmatter with `status: passed`
3. File contains all 5 observable truths with VERIFIED status
4. File contains SEO-01 as SATISFIED in requirements coverage
5. File contains human_verification item for backfill script
6. Evidence cites specific file paths and line numbers (not vague "exists" claims)
   </verify>
   <done>
   01-VERIFICATION.md exists with all 5 Phase 01 success criteria verified against the actual codebase with specific file paths, line numbers, and code patterns. SEO-01 appears as SATISFIED. Human verification item included for backfill script execution with the "database was emptied" context note.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Update all 38 REQUIREMENTS.md checkboxes and traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` and make two mechanical updates:

**Update 1: Flip all 38 v1 requirement checkboxes from `[ ]` to `[x]`**

In the "v1 Requirements" section, change every instance of `- [ ] **REQ-ID**:` to `- [x] **REQ-ID**:` for all 38 requirements:

- SEO-01 through SEO-09 (9)
- TRST-01 through TRST-07 (7)
- ENGR-01 through ENGR-07 (7)
- EMAL-01 through EMAL-06 (6)
- LNCH-01 through LNCH-04 (4)
- INFR-01 through INFR-05 (5)
  Total: 38

**Update 2: Flip all 38 traceability table rows from `Pending` to `Satisfied`**

In the "Traceability" section, change every row's Status column from `Pending` to `Satisfied`.

**Update 3: Update the "Last updated" footer line**

Change `_Last updated: 2026-03-11 after FB Marketplace research — 38 requirements mapped_` to `_Last updated: 2026-03-14 — all 38 v1 requirements verified and marked satisfied_`

Do NOT change:

- v2 Requirements section (leave as-is)
- Out of Scope section (leave as-is)
- Coverage summary counts (they remain correct: 38 total, 38 mapped, 0 unmapped)
- Any SUMMARY.md files (per user decision)
- Any requirement descriptions or phase assignments
  </action>
  <verify>

1. Count `[x]` occurrences in v1 Requirements section — should be exactly 38
2. Count `[ ]` occurrences in v1 Requirements section — should be exactly 0
3. Count `Satisfied` occurrences in Traceability table — should be exactly 38
4. Count `Pending` occurrences in Traceability table — should be exactly 0
5. v2 Requirements section unchanged (no checkboxes added)
6. Out of Scope section unchanged

Run: `grep -c '\[x\]' .planning/REQUIREMENTS.md` should return 38
Run: `grep -c 'Satisfied' .planning/REQUIREMENTS.md` should return 38
</verify>
<done>
All 38 REQUIREMENTS.md checkboxes show `[x]`. All 38 traceability table rows show `Satisfied`. No v2 or out-of-scope content was modified.
</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Phase 01 has 01-VERIFICATION.md with `status: passed` and all 5 truths VERIFIED
2. SEO-01 is no longer orphaned — it appears as SATISFIED in the verification report
3. All 38 REQUIREMENTS.md checkboxes are `[x]`
4. All 38 traceability rows show `Satisfied`
5. No SUMMARY.md files were modified
6. No source code files were modified (documentation-only phase)
</verification>

<success_criteria>

- `grep -c 'VERIFIED' .planning/phases/01-slug-migration/01-VERIFICATION.md` returns 5 (all observable truths)
- `grep 'status: passed' .planning/phases/01-slug-migration/01-VERIFICATION.md` matches
- `grep 'SEO-01.*SATISFIED' .planning/phases/01-slug-migration/01-VERIFICATION.md` matches
- `grep -c '\[x\]' .planning/REQUIREMENTS.md` returns 38
- `grep -c 'Satisfied' .planning/REQUIREMENTS.md` returns 38
- `git diff --name-only` shows only .planning/ files modified, no src/ files
  </success_criteria>

<output>
After completion, create `.planning/phases/07-verification-and-requirements-closure/07-01-SUMMARY.md`
</output>
